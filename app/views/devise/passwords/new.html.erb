<h2 class="mb-4">パスワードを忘れた場合（仮のviewです。機能はMVP後実装予定）</h2>

<%= render "devise/shared/error_messages" %>

<p>登録済みのメールアドレスを入力すると、パスワード再設定用のリンクを送信します。</p>

<%= form_for(resource, as: resource_name, url: password_path(resource_name)) do |f| %>
  <div class="mb-3">
    <%= f.label :email, "メールアドレス", class: "form-label" %>
    <%= f.email_field :email, autofocus: true, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.submit "パスワード再設定リンクを送信", class: "btn btn-primary w-100" %>
  </div>
<% end %>

<%= link_to "ログインに戻る", new_session_path(resource_name), class: "text-decoration-none" %>
