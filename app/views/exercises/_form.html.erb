<%= turbo_frame_tag dom_id(@exercise) do %>
  <% if flash.now[:alert].present? %>
    <div class="alert alert-danger mb-2">
      <%= flash.now[:alert] %>
    </div>
  <% end %>
  
  <%= form_with(model: @exercise, local: false) do |f| %>
    <style>
      @media (min-width: 992px) {
        .exercise-form-actions .btn { min-width: 140px; }
      }
      .exercise-form-actions { display: inline-flex; gap: .5rem; align-items: center; }
    </style>

    <div class="list-group-item d-flex align-items-center gap-2">
      <%= f.text_field :name, class: "form-control", placeholder: "種目名" %>
      <%= f.hidden_field :category_id, value: @exercise.category_id %>

      <div class="exercise-form-actions">
        <%= f.submit "更新", class: "btn btn-sm btn-success" %>
        <%= link_to "キャンセル",
              exercises_path(category_id: @exercise.category_id),
              class: "btn btn-secondary btn-sm",
              data: { turbo: false } %>
      </div>
    </div>
  <% end %>
<% end %>
